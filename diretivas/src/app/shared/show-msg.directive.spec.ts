import { ShowMsgDirective } from './show-msg.directive';

describe('ShowMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMsgDirective();
    expect(directive).toBeTruthy();
  });
});
